<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- Главные Activities -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".activities.MenuActivity"
            android:exported="false" />

        <!-- Admin Activities -->
        <activity
            android:name=".activities.AdminMenuActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AdminTicketsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AdminProfilesActivity"
            android:exported="false" />

        <activity
            android:name=".activities.AdminPartsActivity"
            android:exported="false" />

        <!-- User Activities -->
        <activity
            android:name=".activities.MyTicketsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.PartsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".activities.BookingActivity"
            android:exported="false" />

        <activity
            android:name=".activities.RepairTicketActivity"
            android:exported="false" />

        <activity
            android:name=".activities.PaintingTicketActivity"
            android:exported="false" />

        <activity
            android:name=".activities.QuestionActivity"
            android:exported="false" />

        <activity
            android:name=".activities.TicketTypeActivity"
            android:exported="false" />

    </application>

</manifest>